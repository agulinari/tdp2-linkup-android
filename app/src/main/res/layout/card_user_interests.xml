<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center_horizontal"
    android:layout_marginLeft="@dimen/profile_card_side_margin"
    android:layout_marginRight="@dimen/profile_card_side_margin"
    android:backgroundTint="@color/profileCard"
    android:orientation="horizontal"
    card_view:cardElevation="4dp">

    <co.lujun.androidtagview.TagContainerLayout
            android:id="@+id/tagcontainerLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="0dp"
            android:padding="10dp"
            app:container_border_width="0dp"
            app:tag_border_color="@color/colorPrimary"
            app:tag_background_color="@color/transparent"
            app:container_background_color="@color/profileCard"
            app:container_enable_drag="false"
            app:horizontal_interval="10dp"
            app:vertical_interval="10dp"
            app:tag_clickable="false"
            app:tag_max_length="50"
            app:tag_theme="none" />
</android.support.v7.widget.CardView>
